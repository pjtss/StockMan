import { NextResponse } from "next/server";
import { scanMultiTimeframeBbRebound } from "@/lib/multi-timeframe-bb-rebound";
export async function GET(request: Request) { const secret = process.env.CRON_SECRET?.trim(); const supplied = request.headers.get("x-cron-secret") || request.headers.get("authorization")?.replace(/^Bearer\s+/i, ""); if (!secret || supplied !== secret) return NextResponse.json({ ok: false, error: "UNAUTHORIZED" }, { status: 401 }); const p = new URL(request.url).searchParams; const market = p.get("market")?.toUpperCase() === "KR" ? "KR" : "US"; const limit = Number(p.get("limit") ?? 30); try { return NextResponse.json(await scanMultiTimeframeBbRebound(market, limit)); } catch (e) { return NextResponse.json({ ok: false, error: e instanceof Error ? e.message : String(e) }, { status: 500 }); } }
