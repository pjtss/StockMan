import { NextResponse } from "next/server"; import { createComment, deleteComment } from "@/lib/inquiries"; import { getRequestIdentity } from "@/lib/request-identity"; import { requireAdminSession } from "@/lib/admin-auth";
import { validateCommentInput } from "@/lib/inquiry-validation";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const body=await request.json().catch(()=>({}));const input=validateCommentInput(body.content);if(!input.ok)return NextResponse.json({error:input.error},{status:400});await createComment(Number((await params).id),input.content,getRequestIdentity(request));return NextResponse.json({ok:true},{status:201});}
export async function DELETE(request:Request){if(!(await requireAdminSession()))return NextResponse.json({error:"UNAUTHORIZED"},{status:401});const id=Number(new URL(request.url).searchParams.get("commentId"));if(!Number.isInteger(id)||id<1)return NextResponse.json({error:"INVALID_COMMENT"},{status:400});await deleteComment(id);return NextResponse.json({ok:true});}
