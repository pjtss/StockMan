import { NextResponse } from "next/server";
import { deleteInquiry, getInquiry } from "@/lib/inquiries";
import { requireAdminSession } from "@/lib/admin-auth";
import { maskIp, summarizeUserAgent } from "@/lib/request-identity";
export async function GET(_: Request,{params}:{params:Promise<{id:string}>}) { const id=Number((await params).id); if(!Number.isInteger(id)||id<1)return NextResponse.json({error:"NOT_FOUND"},{status:404}); try { const row=await getInquiry(id); if(!row)return NextResponse.json({error:"NOT_FOUND"},{status:404}); return NextResponse.json({...row,ip_address:maskIp(row.ip_address),user_agent:summarizeUserAgent(row.user_agent),comments:row.comments.map((c: { ip_address: string; user_agent: string })=>({...c,ip_address:maskIp(c.ip_address),user_agent:summarizeUserAgent(c.user_agent)}))}); } catch { return NextResponse.json({error:"INQUIRY_UNAVAILABLE"},{status:503}); } }
export async function DELETE(_: Request,{params}:{params:Promise<{id:string}>}) { if (!(await requireAdminSession())) return NextResponse.json({error:"UNAUTHORIZED"},{status:401}); const id=Number((await params).id); if(!Number.isInteger(id)||id<1)return NextResponse.json({error:"INVALID_INQUIRY"},{status:400}); try { await deleteInquiry(id); return NextResponse.json({ok:true,deleted:true}); } catch { return NextResponse.json({error:"INQUIRY_DELETE_FAILED"},{status:503}); } }
