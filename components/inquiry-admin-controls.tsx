"use client"; import { useState } from "react"; import { useRouter } from "next/navigation";
export function InquiryAdminControls({id,isNotice}:{id:number;isNotice:boolean}){const router=useRouter();const[busy,setBusy]=useState(false);async function toggle(){setBusy(true);try{const r=await fetch(`/api/inquiries/${id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({isNotice:!isNotice})});if(r.ok)router.refresh();}finally{setBusy(false);}}return <div className="inquiryAdminBar"><strong>관리자 모드</strong><button type="button" onClick={()=>void toggle()} disabled={busy}>{isNotice?"공지 해제":"공지로 설정"}</button></div>}
