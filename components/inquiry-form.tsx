"use client"; import { useState } from "react"; import { useRouter } from "next/navigation";
export function InquiryForm(){const router=useRouter();const[title,setTitle]=useState(""),[content,setContent]=useState(""),[error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/inquiries",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({title,content})});const j=await r.json();if(!r.ok){setError(j.error||"등록에 실패했습니다.");return}router.push(`/inquiries/${j.id}`)}return <form className="inquiryForm" onSubmit={submit}><label>제목<input value={title} maxLength={100} onChange={e=>setTitle(e.target.value)} required/></label><label>내용<textarea value={content} maxLength={5000} onChange={e=>setContent(e.target.value)} required rows={10}/></label>{error&&<p role="alert">{error}</p>}<button type="submit">문의 등록</button></form>}
