"use client"; import Link from "next/link"; import { useState } from "react"; import { useRouter } from "next/navigation";
export function InquiryForm(){const router=useRouter();const[title,setTitle]=useState(""),[content,setContent]=useState(""),[error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/inquiries",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({title,content})});const j=await r.json();if(!r.ok){setError(j.error||"등록에 실패했습니다.");return}router.push(`/inquiries/${j.id}`)}return <form className="inquiryForm" onSubmit={submit}><div className="inquiryFormIntro"><span>새 문의</span><p>제목과 내용을 입력하면 누구나 확인할 수 있습니다.</p></div><label>제목<input value={title} maxLength={100} onChange={e=>setTitle(e.target.value)} placeholder="문의 제목을 입력하세요" required/></label><label>내용<textarea value={content} maxLength={5000} onChange={e=>setContent(e.target.value)} placeholder="문의 내용을 입력하세요" required rows={10}/></label>{error&&<p role="alert">{error}</p>}<div className="inquiryFormActions"><Link href="/inquiries">취소</Link><button type="submit">문의 등록</button></div></form>}
